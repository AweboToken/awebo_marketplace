import DropsPageClient from '@/components/drops/DropsPageClient';

export const metadata = {
  title: 'Drops — AWEBO',
  description: 'Creator drops, physical products, and token-backed collections.',
};

export default function DropsPage() {
  return <DropsPageClient />;
}
