export const metadata = {
  title: 'Launch Brand — AWEBO Creator Studio',
  description: 'Create your creator profile, token, and merch. All prices in ETH.',
};

export default function LaunchLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
